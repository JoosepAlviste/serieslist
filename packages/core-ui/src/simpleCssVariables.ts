/**
 * The CSS variable that can be used to globally configure SVG colors. This
 * is NOT a vanilla extract variable, so the usege is a bit different and it
 * can be used in some places where Vanilla Extract variables aren't available
 * (e.g., Vite configuration).
 */
export const SVG_COLOR_VAR = '--icon-color'
